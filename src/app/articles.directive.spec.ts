import { ArticlesDirectiveDirective } from './articles-directive.directive';

describe('ArticlesDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ArticlesDirective();
    expect(directive).toBeTruthy();
  });
});
